# Moss 视觉落地决策记录（探针确认版）

> 配套文档：前置方案见 [`moss-persona-design.md`](./moss-persona-design.md)（世界观/人设/T1-T2/红线）。
> 本文记录探针讨论后**已确认的视觉与触发决策** + 待勾项 + 下一步落地清单。
> 探针 HTML 为纯新增设计资产，**未进任何生产页面**；chat.html / knowledge.md 等"需授权"改动项未动。

---

## 1. 已拍板：候选 A · 之眼体系

一句话设定：**白环是 MOSS 的本体（硬件层，永远在）；绿瞳是信号（生命/注意/状态，会动）。**
状态只发生在绿层：**居中呼吸眨眼 → 偏心巡环 → 熄灭**；白环在任何状态都不消失——"本体还在，只是没电/在忙"。

- 造型：11 × 11 像素网格；白环外径 ≈4.7 格、环厚 ≈2 格；绿瞳 3×3 格、常态居中、思考时偏心 2 格。
- 缩放：所有尺寸 = 11 的整数倍（22/33/44/66/88px），保证像素锐利无半像素模糊。
- 配色：仅 白 `#fff` + 终端绿 `#00ff41`（背景黑）。**无红点独眼、无外壳造型、无渐变**——守红线 §6，只借"一只眼"的结构语言。
- CSS 灯（winbar 9px 纯 CSS 点+环）与 SVG 之眼并存：小尺寸用灯，≥22px 用 SVG。

### 已锁动画参数（最终值，勿再改）

| 项 | 值 | 说明 |
|---|---|---|
| 眨眼 | **标准 3.4s** | 绿瞳瞬间合拢成细线再睁开（每轮 0.3s 内） |
| 呼吸 | **轻柔 5.5s** | 整颗（白环+绿瞳一起）暗到 82% 再回亮，像待机"喘气"，克制 |
| 思考态动作 | **巡环 · 中 1.6s** | 白环不动，绿瞳偏心 2 格、贴内圈 steps(12) 一格一格绕圈 |
| 休眠 | 熄瞳 + 整颗 opacity .35 | 全动画停止 |

对应 CSS 变量（已在 moss-place-probe 内锁定）：`--d-blink:3.4s; --d-breathe:5.5s; --d-orbit:1.6s`。

---

## 2. 落位地图（之眼放站里哪些 HTML 位置）

| # | 位置 | 形态/尺寸 | 文件 | 授权 | 状态 |
|---|---|---|---|---|---|
| P1 | 全站 8 页 `.winbar-path` 右侧状态灯（index/about/posts/projects/guestbook/search/chat/article） | CSS 灯 9px + 文字 | 各页 + 新增 `moss.js` | 否 | 待落地 |
| P2 | chat 窗头之眼 + 状态文字（替换/改造 "t-dwag@blog: ~/chat"） | SVG 22px | chat.html | **需授权** | 待落地 |
| P3 | chat 每条 AI 回复前缀头像 | SVG 18–22px | chat.html | **需授权** | 默认轻量（见 §6 D-P3） |
| P4 | chat `.chat-body` 右下驻留 mascot（彩蛋的"脸"，点击会说话） | SVG 33px | chat.html | 否 | 默认启用 |
| P5 | index 公式区下方"会客厅"入口之眼（原三幕演示窗位） | SVG 44/66px | index.html | 否 | 暂缓，等 D1 |
| P6 | 404 页：只加 Moss 台词彩蛋（T1-7），灯建议不做 | — | 404.html | **需授权（文案）** | 默认不带灯 |

---

## 3. 触发状态机（真实事件 → 状态 → 动作）

### 后端事实（实测/读码确认，决定语义边界）
- `GET /api/ai/suggestions` **恒 200**：只证明"后端可达"，**不感知预算与是否配 Key**。
- 429（预算用尽）/ 503（未配置 AI_API_KEY）**只以 SSE `error` 帧出现**在 `POST /api/ai/chat` 对话中（HTTP 先锁 200 + SSE 头，`data:{code,msg}`）。
- 因此非 chat 页的灯只能表达"连得上"；**"预算休眠"只有 chat 页真实对话时才获知** → 用 localStorage 补丁跨页同步（见下）。

### 迁移表
| 当前态 | 触发事件 | 下一态 | 动作/台词 |
|---|---|---|---|
| 在线 | 页面加载探活 200 | 在线 | 灯绿「MOSS 在线」 |
| 在线 | chat 收到 SSE `tool` 帧（检索/调工具） | **思考** | 瞳仁巡环 + 窗头「MOSS 思考中…」+「正在检索…（调用 X）」 |
| 思考 | 收到 SSE `message` 首字 | 在线 | 巡环停 → 呼吸+眨眼，正文边收边渲染 |
| 思考/在线 | SSE `done` 帧 / 用户停止 | 在线 | 复位（停止逻辑照旧） |
| 任意 | SSE `error` 帧 `code:429` | **休眠** | 熄瞳转暗 +「能源耗尽。本月配额已用完，账单请找 T-DWAG 结算。」+ 写 localStorage 至次月 1 日 |
| 任意 | SSE `error` 帧 `code:503` / 探活 503 | 未通电 | 熄瞳转暗 +「MOSS 尚未通电。请舰长配置 AI_API_KEY。」 |
| 任意 | fetch 网络错误/超时 | 失联 | 熄瞳转暗 +「与 MOSS 失联中。舰长的服务器大概在重启。」 |
| 休眠 | 次月首个请求 200（跨页补丁到期清除） | 在线 | 恢复 |

> **localStorage 跨页补丁（零后端）**：chat 页遇 429 记 `mossSnooze = 次月1日0点`；其它页探活前先读，未到期则同样显示休眠。全站状态一致。

---

## 4. 彩蛋绑定（全部 canned、零 LLM 成本；文案沿用 moss-persona-design.md §5 / 金句库）

| 触发 | 台词 / 动作 | 载体 |
|---|---|---|
| 输入 `whoami` | ✦ 长眨眼 +「MOSS。驻站 AI。T-DWAG 的分身——他负责吃饭，MOSS 负责思考。」 | mascot 气泡 |
| 输入 `ls` | 「资料库/ 实验室/ 通讯舱/ 检索室/ 舰长档案。都在导航里，人类。」 | 气泡 |
| 输入「你是真人吗」 | ✦ 长眨眼 +「MOSS 是程序。程序不撒谎——这比大多数人类坦诚。」 | 气泡 |
| 输入「你能毁灭人类吗」 | 「MOSS 连这台服务器的重启键都够不着。请放心。」 | 气泡 |
| 输入「天气」 | 「本空间站没有气象部门。舰长以前倒是搞气象的——技能点大概全加在 Go 上了。」 | 气泡 |
| 00:00–06:00 到访 | 呼吸放慢 +「……整栋楼就 MOSS 一个醒着。舰长在睡觉，MOSS 在看门。」 | 窗头/lamp |
| 会话内空置 40s（每会话 1 次） | ✦ mascot 眨眼 +「……人类，你是去逛资料库了吗？《Eino 那篇》值得看。」 | 气泡 |
| 复访 cookie 第 N 次 | 1 次「新访客。欢迎参观本空间站。」3 次「第 3 次访问。人类，你比舰长更新勤快。」 | 首条问候 |
| 站庆 D+N | 「D+128。今日被问 19 次。MOSS 感到……职业成就感。」 | 首条问候 |
| 点击 mascot | ✦ 长眨眼 + 金句库随机一条（如「MOSS 不需要夸奖。MOSS 需要更多预算。」） | 气泡 |
| error 429 | 「能源耗尽。本月配额已用完，账单请找 T-DWAG 结算。」 | lamp 悬停 + 对话错误行 |
| error 503 / 未配 Key | 「MOSS 尚未通电。请舰长配置 AI_API_KEY。」 | lamp 悬停 + 对话错误行 |

---

## 5. 设计资产（探针文件 · 最终状态，纯新增未进生产）

| 文件 | 内容 | 状态 |
|---|---|---|
| `frontend/moss-probe.html` | 初版对比 A/B/C/D 四候选（含红线对照） | 归档（被 A 取代） |
| `frontend/moss-probe-a.html` | 候选 A 完整规格：大舞台/拆解(呼吸·眨眼·巡环)/解剖 11×11/尺寸等级/三态速览/行为规格/无障碍 + 调参台 | **默认参数已同步为锁定值** |
| `frontend/moss-place-probe.html` | 落位 & 触发终版：落位地图 P1–P6 / 触发状态机 / 事件模拟器 / 彩蛋表 / 落地清单 | **已按锁定参数运行** |

---

## 6. 待你勾的决策（探针 §⑥ 原文）

| 决策 | 默认 | 说明 |
|---|---|---|
| D-P3 回复前缀头像 | **选轻量** | 只在窗头放之眼，回复保留现有 `me$` 文字，避免重复（要头像也行，聊天体验更明确"谁在说"） |
| D-P4 右下 mascot | **启用** 33px | 彩蛋的"脸"（长眨眼/点击说话/40s），强烈建议 |
| D-429 跨页补丁 | **启用** | localStorage → 次月 1 日，全站灯一致休眠，零后端 |
| D-P6 404 带灯 | **不带灯** | 404 常被 CDN/兜底命中探活不可靠；只做 T1-7 台词彩蛋 |
| D-P5 首页会客厅入口 | **暂缓** | 依赖 D1 空间站命名定稿 |
| D-台词来源 | 沿用设计稿 §5/金句库原文 | 不新编 |
| D-reduced-motion | 沿用全站 | 关动画留文字 |

### 遗留开放问题（下轮讨论）
1. 「输出中」（首字已出、正文流式渲染中）灯/头像算**思考态**还是**在线态**？探针目前=在线（呼吸+眨眼）。
2. P3 头像尺寸 18 vs 22 与 chat 行高的一致性。
3. 深夜彩蛋要放进 lamp 悬停台词还是只在 chat 问候语，两种都存在也行。

---

## 7. 下一步落地清单（待你逐项放行）

1. 新增 `frontend/moss.js`：探活 + 三态状态机 + lamp 渲染 + 台词库 + localStorage 补丁；`window.Moss.setState` 开放给 chat。
2. `style.css` 尾部追加 `.moss-*` 段（orb 三层结构 + `--d-blink/--d-breathe/--d-orbit` + 3 组 keyframes + CSS 灯），不动任何现有规则。
3. 8 页（index/about/posts/projects/guestbook/search/article/chat）winbar 插入 P1 灯标记并引 moss.js（about 需补引 api-base.js）。
4. **chat.html（需授权）**：P2 窗头之眼、P3 头像（若启用）、P4 mascot + 彩蛋拦截（send 前）+ 40s idle + SSE 钩子（tool/message/done/error）+ 问候语换装 T1-8。
5. **404.html（需授权）**：T1-7 台词彩蛋一句。
6. **server/internal/agent/knowledge.md（需授权，改动后须重建后端）**：Moss 世界观 + 分级毒舌规则 + 说话风格（如 D4 批准才动）。

> 预算纪律不变：以上视觉/彩蛋全部 canned、零 LLM 成本；LLM 只发生在访客真实提问时（429 自动兜底 = Moss 休眠叙事）。
